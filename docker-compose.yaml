services:
  ac-front:
    build: ./AC-Front
    ports:
      - "80:80"
    depends_on:
      - ac-back

  ac-back:
    build: ./AC-Back
    ports:
      - "5000:5000"
    env_file:
      - ./AC-Back/ac-back-db.env